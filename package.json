{
  "name": "swiftdl",
  "version": "3.0.0",
  "description": "Video & Audio Downloader powered by yt-dlp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pip install yt-dlp || pip3 install yt-dlp || true"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
